# Notification App Backend

Backend directory included for the required repository structure.

The frontend task consumes the provided notification API directly through the Vite development proxy, so no custom backend service is required for this stage.
